"use client";

import { FormEvent, RefObject, useState } from "react";
import { Button } from "@/components/ui/Button";
import { SelectMenu } from "@/components/ui/SelectMenu";
import { ServiceCategoryDetailsFields } from "@/components/services/ServiceCategoryDetailsFields";
import { serviceCategories, type ServiceCategoryId } from "@/data/services";

type PhotoPreview = { id: string; file: File; url: string };

type Props = {
  isKorean: boolean;
  locale: string;
  category: ServiceCategoryId | "";
  categoryLabels: Record<ServiceCategoryId, string>;
  onCategoryChange: (category: ServiceCategoryId) => void;
  detailValues: Record<string, string>;
  onDetailChange: (key: string, value: string) => void;
  logo: PhotoPreview | null;
  photos: PhotoPreview[];
  primaryPhotoId: string | null;
  logoInputRef: RefObject<HTMLInputElement | null>;
  photoInputRef: RefObject<HTMLInputElement | null>;
  onLogoAdd: (files: FileList | null) => void;
  onLogoRemove: () => void;
  onPhotosAdd: (files: FileList | null) => void;
  onPhotoRemove: (id: string) => void;
  onPrimaryPhotoChange: (id: string) => void;
  onSubmit: (event: FormEvent<HTMLFormElement>) => void;
  onInvalid: (message: string) => void;
  onPreview: () => void;
  onInput: (form: HTMLFormElement) => void;
  acceptedTerms: boolean;
  onTermsChange: (accepted: boolean) => void;
  isSubmitting: boolean;
  notice: string;
  serviceArea: string;
  suburb: string;
  allAreasValue: string;
  areaOptions: Array<{ value: string; label: string }>;
  suburbOptions: Array<{ value: string; label: string }>;
  onAreaChange: (value: string) => void;
  onSuburbChange: (value: string) => void;
};

const languages = ["English", "한국어", "中文", "हिन्दी", "日本語"];

function StepHeading({ number, title, help }: { number: number; title: string; help: string }) {
  return <header className="service-editor-heading"><span>{number}</span><h2>{title}</h2><small>{help}</small></header>;
}

function FieldHint({ children }: { children: string }) {
  return <small className="service-editor-field-hint">{children}</small>;
}

function BusinessInformationEditor({ isKorean, areaOptions, serviceArea, onAreaChange, suburb }: Pick<Props, "isKorean" | "areaOptions" | "serviceArea" | "onAreaChange" | "suburb">) {
  const [hours, setHours] = useState([{ id: crypto.randomUUID(), days: ["monday"], opens: "09:00", closes: "17:00" }]);
  const dayOptions = isKorean ? [["monday", "월요일"], ["tuesday", "화요일"], ["wednesday", "수요일"], ["thursday", "목요일"], ["friday", "금요일"], ["saturday", "토요일"], ["sunday", "일요일"], ["public-holidays", "공휴일"]] : [["monday", "Monday"], ["tuesday", "Tuesday"], ["wednesday", "Wednesday"], ["thursday", "Thursday"], ["friday", "Friday"], ["saturday", "Saturday"], ["sunday", "Sunday"], ["public-holidays", "Public holidays"]];
  const updateHour = (id: string, key: "opens" | "closes", value: string) => setHours((current) => current.map((row) => row.id === id ? { ...row, [key]: value } : row));
  const toggleDay = (rowId: string, day: string) => setHours((current) => current.map((row) => row.id === rowId ? { ...row, days: row.days.includes(day) ? row.days.filter((item) => item !== day) : [...row.days, day] } : row));
  const hoursValue = hours.map((row) => `${row.days.map((day) => dayOptions.find(([value]) => value === day)?.[1]).filter(Boolean).join(", ")} ${row.opens}–${row.closes}`).join(" · ");
  const copy = isKorean ? { title: "업체 정보", help: "고객이 신뢰하고 연락할 수 있는 정보입니다.", location: "업체 위치", address: "상세 주소", visibility: "주소 공개 설정", areaOnly: "위치만 표시", exact: "상세 주소 표시", phone: "전화번호", email: "이메일", website: "웹사이트", year: "설립 연도", hours: "영업시간", add: "영업시간 추가", remove: "삭제", languages: "제공 가능 언어" } : { title: "Business information", help: "Use contact details customers can rely on.", location: "Location", address: "Street address", visibility: "Address visibility", areaOnly: "Show location only", exact: "Show exact address", phone: "Phone", email: "Email", website: "Website", year: "Established year", hours: "Opening hours", add: "Add opening hours", remove: "Remove", languages: "Languages spoken" };
  return <section className="service-editor-card service-editor-business-rebuilt"><StepHeading number={4} title={copy.title} help={copy.help} /><div className="service-editor-business"><SelectMenu id="business-location" name="business-location" label={copy.location} placeholder={isKorean ? "위치 선택" : "Select location"} options={areaOptions} value={serviceArea} onChange={onAreaChange} required /><label className="service-editor-address">{copy.address}<input name="service-address" minLength={5} maxLength={200} placeholder="33c Bruce McLaren Rd" /><FieldHint>{isKorean ? "선택 · 입력 시 5~200자" : "Optional · 5–200 characters if provided"}</FieldHint></label><fieldset className="service-editor-address-visibility"><legend><span className="service-editor-required-mark">*</span>{copy.visibility}</legend><label><input type="radio" name="address-visibility" value="area" defaultChecked />{copy.areaOnly}</label><label><input type="radio" name="address-visibility" value="exact" />{copy.exact}</label></fieldset><label>{copy.phone}<input name="service-phone" type="tel" required pattern="\\+?[0-9 ()-]{7,25}" placeholder="021 0266 6545" /><FieldHint>{isKorean ? "필수 · 숫자 7자리 이상" : "Required · at least 7 digits"}</FieldHint></label><label>{copy.email}<input name="service-email" type="email" placeholder="hello@example.co.nz" /><FieldHint>{isKorean ? "선택 · 유효한 이메일 주소" : "Optional · valid email address"}</FieldHint></label><label>{copy.website}<input name="service-website" type="url" placeholder="https://example.co.nz" /><FieldHint>{isKorean ? "선택 · https://를 포함해 입력" : "Optional · include https://"}</FieldHint></label><label>{copy.year}<input name="service-founded-year" type="number" min="1800" max={new Date().getFullYear()} placeholder="2021" /><FieldHint>{isKorean ? `선택 · 1800~${new Date().getFullYear()}년` : `Optional · 1800–${new Date().getFullYear()}`}</FieldHint></label></div><fieldset className="service-editor-hours-rebuilt"><legend>{copy.hours}</legend><p className="service-editor-hours-note"><i className="ms ms-info" aria-hidden="true" />{isKorean ? <>선택하지 않은 요일은 <strong>휴무</strong>로 표시됩니다.</> : <>Days not selected will be shown as <strong>closed</strong>.</>}</p><input type="hidden" name="service-weekday-hours" value={hoursValue} />{hours.map((row) => { const selectedLabels = row.days.map((day) => dayOptions.find(([value]) => value === day)?.[1]).filter(Boolean); return <div key={row.id}><details className="service-editor-days-picker"><summary>{selectedLabels.length ? selectedLabels.join(", ") : (isKorean ? "요일 선택" : "Select days")}</summary><div>{dayOptions.map(([value, label]) => { const selectedElsewhere = hours.some((other) => other.id !== row.id && other.days.includes(value)); return <label key={value}><input type="checkbox" checked={row.days.includes(value)} disabled={selectedElsewhere} onChange={() => toggleDay(row.id, value)} /><span>{label}</span></label>; })}</div></details><input type="time" value={row.opens} onChange={(event) => updateHour(row.id, "opens", event.target.value)} /><input type="time" value={row.closes} onChange={(event) => updateHour(row.id, "closes", event.target.value)} /><button type="button" onClick={() => setHours((current) => current.length === 1 ? current : current.filter((item) => item.id !== row.id))} aria-label={copy.remove}><i className="ms ms-delete" aria-hidden="true" /></button></div>; })}<button type="button" onClick={() => setHours((current) => [...current, { id: crypto.randomUUID(), days: [], opens: "09:00", closes: "17:00" }])}><i className="ms ms-add" aria-hidden="true" /> {copy.add}</button></fieldset><fieldset className="service-editor-languages"><legend>{copy.languages}</legend>{languages.map((language, index) => <label key={language}><input type="checkbox" name="service-languages" value={language} defaultChecked={index === 0} />{language}</label>)}<button type="button"><i className="ms ms-add" aria-hidden="true" /> {isKorean ? "언어 추가" : "Add language"}</button></fieldset><input type="hidden" name="service-area-values" value={[serviceArea, suburb].filter(Boolean).join("|")} /></section>;
}

export function ServiceCreateEditor(props: Props) {
  const { isKorean, category, categoryLabels } = props;
  const [selectedAreas, setSelectedAreas] = useState<string[]>(() => [props.serviceArea, props.suburb].filter(Boolean));
  const text = isKorean ? {
    identity: "제공자 정보", about: "서비스 소개", details: "서비스 상세 및 가격", business: "업체 정보", gallery: "작업 사진", area: "서비스 지역", safety: "안전 기준", category: "서비스 종류", logo: "로고", provider: "업체/제공자명", title: "서비스명", type: "제공자 유형", local: "지역 업체", sole: "개인 사업자", summary: "짧은 요약", description: "서비스 소개", phone: "전화번호", email: "이메일", website: "웹사이트", hours: "영업시간", year: "설립 연도", languages: "제공 가능 언어", address: "상세 주소", addressVisibility: "주소 공개 설정", publish: "검토 및 등록", preview: "업체 카드 미리보기", save: "임시 저장", upload: "로고 업로드", photos: "대표 사진과 추가 작업 사진을 올려 주세요", terms: "이용약관 및 커뮤니티 가이드라인에 동의합니다.", areaHelp: "고객에게 제공 가능한 지역을 설정하세요.", safetyHelp: "등록 전 Tada 안전 기준을 확인해 주세요.", identityHelp: "서비스 페이지 상단에 표시됩니다.", aboutHelp: "고객이 서비스를 빠르게 이해할 수 있게 작성하세요.", detailsHelp: "서비스 페이지의 상세 및 가격 영역에 표시됩니다.", businessHelp: "고객이 연락하고 신뢰할 수 있는 정보입니다.", galleryHelp: "실제 작업 사진은 문의 전환에 도움이 됩니다.", reviewed: "모든 서비스는 공개 전 검토됩니다.", contact: "정확한 연락처가 필요합니다.", genuine: "실제 제공하는 서비스만 등록할 수 있습니다.", prohibited: "위험하거나 금지된 서비스는 등록할 수 없습니다.", submit: "서비스 등록 신청",
  } : {
    identity: "Provider identity", about: "About", details: "Service details & pricing", business: "Business information", gallery: "Work gallery", area: "Service area", safety: "Trust & safety requirements", category: "Service category", logo: "Logo", provider: "Business / provider name", title: "Service title", type: "Provider type", local: "Local business", sole: "Sole trader", summary: "Short summary", description: "About your service", phone: "Phone", email: "Email", website: "Website", hours: "Opening hours", year: "Established year", languages: "Languages spoken", address: "Street address", addressVisibility: "Address visibility", publish: "Review & publish", preview: "Preview provider card", save: "Save draft", upload: "Upload logo", photos: "Add a cover image and photos of your work", terms: "I agree to Tada’s Terms and Community Guidelines.", areaHelp: "Set the places where you provide this service.", safetyHelp: "Review Tada’s requirements before submitting.", identityHelp: "These details appear at the top of your public service page.", aboutHelp: "Help customers quickly understand the service you offer.", detailsHelp: "This powers the public service details and pricing section.", businessHelp: "Use contact information customers can rely on.", galleryHelp: "Real work photos help customers choose with confidence.", reviewed: "All listings are reviewed before going live.", contact: "Accurate contact details are required.", genuine: "List active services you genuinely provide.", prohibited: "Unsafe or prohibited services are not allowed.", submit: "Submit service listing",
  };

  const handleSubmit = (event: FormEvent<HTMLFormElement>) => {
    event.preventDefault();
    const form = event.currentTarget;
    if (!category) { props.onInvalid(isKorean ? "서비스 종류를 선택해 주세요." : "Choose a service category to continue."); return; }
    if (!props.acceptedTerms) { props.onInvalid(isKorean ? "이용약관 및 커뮤니티 가이드라인에 동의해 주세요." : "Agree to the Terms and Community Guidelines to continue."); return; }
    if (!form.checkValidity()) {
      const invalidField = Array.from(form.elements).find((element): element is HTMLInputElement | HTMLTextAreaElement | HTMLSelectElement => (element instanceof HTMLInputElement || element instanceof HTMLTextAreaElement || element instanceof HTMLSelectElement) && !element.disabled && !element.validity.valid);
      invalidField?.focus();
      props.onInvalid(isKorean ? "필수 항목과 입력 형식을 확인해 주세요. 별표(*) 항목은 반드시 입력해야 합니다." : "Check the required fields and input formats. Fields marked with * are required.");
      return;
    }
    props.onSubmit(event);
  };

  return <form className="service-create-editor" noValidate onSubmit={handleSubmit} onInput={(event) => props.onInput(event.currentTarget)}>
    <p className="service-editor-required-note"><span aria-hidden="true">*</span>{isKorean ? " 표시된 항목은 필수입니다." : " indicates a required field."}</p>
    <BusinessInformationEditor isKorean={isKorean} areaOptions={props.areaOptions} serviceArea={props.serviceArea} suburb={props.suburb} onAreaChange={props.onAreaChange} />
    <section className="service-editor-card service-editor-identity"><StepHeading number={1} title={text.identity} help={text.identityHelp} /><div className="service-editor-category"><label><span className="service-editor-required-mark">*</span>{text.category}</label><div>{serviceCategories.map(({ id, icon }) => <button type="button" key={id} className={category === id ? "is-selected" : ""} onClick={() => props.onCategoryChange(id)}><i className={`ms ${icon}`} aria-hidden="true" />{categoryLabels[id]}</button>)}</div></div><div className="service-editor-identity-grid"><div className="service-editor-logo"><label>{text.logo}</label><input ref={props.logoInputRef} id="editor-logo" type="file" accept="image/jpeg,image/png,image/webp" onChange={(event) => { props.onLogoAdd(event.target.files); event.currentTarget.value = ""; }} />{props.logo ? <div><img src={props.logo.url} alt="" /><button type="button" onClick={props.onLogoRemove}><i className="ms ms-close" aria-hidden="true" /></button></div> : <button type="button" onClick={() => props.logoInputRef.current?.click()}><i className="ms ms-image" aria-hidden="true" /><span>{text.upload}</span><small>PNG or JPG, max 2MB</small></button>}</div><div className="service-editor-fields"><label><span className="service-editor-required-mark">*</span>{text.provider}<input name="business-name" required placeholder="SmartLearn Tutoring" /></label><label><span className="service-editor-required-mark">*</span>{text.title}<input name="service-name" required placeholder="Tutoring" /></label><fieldset><legend>{text.type}</legend><label><input type="radio" name="provider-type" value="business" defaultChecked />{text.local}</label><label><input type="radio" name="provider-type" value="sole-trader" />{text.sole}</label></fieldset></div></div></section>

    <section className="service-editor-card"><StepHeading number={2} title={text.about} help={text.aboutHelp} /><div className="service-editor-about"><label><span className="service-editor-required-mark">*</span>{text.summary}<textarea className="service-editor-summary" name="service-summary" required maxLength={100} placeholder={isKorean ? "어떤 도움을 제공하는지 한 문장으로 알려주세요" : "Describe your service in one clear sentence"} /><FieldHint>{isKorean ? "최대 100자 (명함에는 2줄까지 표시)" : "Up to 100 characters (two lines on your card)"}</FieldHint></label><label><span className="service-editor-required-mark">*</span>{text.description}<textarea name="service-description" required minLength={20} maxLength={2000} placeholder={isKorean ? "서비스, 경력, 고객이 기대할 수 있는 결과를 알려주세요." : "Tell customers what you offer, your experience, and what they can expect."} /><FieldHint>{isKorean ? "20~2,000자" : "20–2,000 characters"}</FieldHint></label></div></section>

    <section className="service-editor-card"><StepHeading number={3} title={text.details} help={text.detailsHelp} /><p className="service-editor-details-note"><span className="service-editor-required-mark">*</span>{isKorean ? "서비스 등록은 필수입니다." : "A service is required."}</p><ServiceCategoryDetailsFields key={category} category={category} locale={props.locale} values={props.detailValues} onValueChange={props.onDetailChange} /></section>

    <section className="service-editor-card"><StepHeading number={4} title={text.business} help={text.businessHelp} /><div className="service-editor-business"><label>{text.phone}<input name="service-phone" type="tel" required placeholder="021 0266 6545" /></label><label>{text.email}<input name="service-email" type="email" placeholder="hello@example.co.nz" /></label><label>{text.website}<input name="service-website" type="url" placeholder="www.example.co.nz" /></label><label className="service-editor-address">{text.address}<input name="service-address" required minLength={5} maxLength={200} placeholder="33c Bruce McLaren Rd" /></label><label>{text.year}<input name="service-founded-year" type="number" min="1800" max={new Date().getFullYear()} placeholder="2021" /></label><div className="service-editor-hours"><span>{text.hours}</span><label><input name="service-weekday-hours" required placeholder={isKorean ? "월–금" : "Mon–Fri"} /><input placeholder="9:00 AM – 5:00 PM" /></label><label><input name="service-saturday-hours" placeholder={isKorean ? "토요일" : "Saturday"} /><input placeholder="9:00 AM – 1:00 PM" /></label><label><input name="service-sunday-hours" placeholder={isKorean ? "일요일·공휴일" : "Sun & public holidays"} /><input placeholder={isKorean ? "휴무" : "Closed"} /></label></div></div><fieldset className="service-editor-languages"><legend>{text.languages}</legend>{languages.map((language, index) => <label key={language}><input type="checkbox" name="service-languages" value={language} defaultChecked={index === 0} />{language}</label>)}<button type="button"><i className="ms ms-add" aria-hidden="true" /> {isKorean ? "언어 추가" : "Add language"}</button></fieldset><fieldset className="service-editor-address-visibility"><legend>{text.addressVisibility}</legend><label><input type="radio" name="address-visibility" value="area" defaultChecked />{isKorean ? "지역과 세부 지역만 표시" : "Show service area only"}</label><label><input type="radio" name="address-visibility" value="exact" />{isKorean ? "상세 주소 표시" : "Show exact address"}</label></fieldset></section>

    <section className="service-editor-card"><StepHeading number={5} title={text.gallery} help={text.galleryHelp} /><p className="service-editor-photo-help">{text.photos}</p><input ref={props.photoInputRef} id="editor-photos" type="file" accept="image/jpeg,image/png,image/webp" multiple onChange={(event) => { props.onPhotosAdd(event.target.files); event.currentTarget.value = ""; }} /><FieldHint>{isKorean ? "선택 · JPG, PNG 또는 WEBP · 사진은 최대 5장" : "Optional · JPG, PNG or WEBP · up to 5 photos"}</FieldHint><div className="service-editor-gallery">{props.photos.map((photo) => <figure key={photo.id} className={photo.id === props.primaryPhotoId ? "is-cover" : ""}><button type="button" onClick={() => props.onPrimaryPhotoChange(photo.id)}><img src={photo.url} alt="" /></button><button type="button" onClick={() => props.onPhotoRemove(photo.id)}><i className="ms ms-close" aria-hidden="true" /></button></figure>)}{props.photos.length < 5 ? <button type="button" className="service-editor-add-photo" onClick={() => props.photoInputRef.current?.click()}><i className="ms ms-add" aria-hidden="true" /><span>{isKorean ? "사진 추가" : "Add photo"}</span></button> : null}</div></section>

    <section className="service-editor-card"><StepHeading number={6} title={text.area} help={text.areaHelp} /><div className="service-editor-area"><SelectMenu id="editor-service-area" name="service-area" label={isKorean ? "주 활동 지역" : "Service area / main location"} placeholder={isKorean ? "지역 선택" : "Select location"} options={props.areaOptions} value={props.serviceArea} onChange={(next) => { props.onAreaChange(next); setSelectedAreas((current) => current.includes(next) ? current : [...current, next]); }} required /><SelectMenu id="editor-service-suburb" name="service-suburb" label={isKorean ? "세부 지역" : "Suburbs / neighbourhoods"} placeholder={isKorean ? "세부 지역 선택" : "Select suburb"} options={[{ value: "__all_suburbs__", label: isKorean ? "전체 세부 지역" : "All suburbs" }, ...props.suburbOptions]} value={props.suburb} onChange={(next) => { props.onSuburbChange(next); setSelectedAreas((current) => current.includes(next) ? current : [...current, next]); }} /><div className="service-editor-area-selection">{selectedAreas.filter((area) => area !== "__all_suburbs__").map((area) => <span key={area}>{area}<button type="button" aria-label={`${area} ${isKorean ? "삭제" : "remove"}`} onClick={() => setSelectedAreas((current) => current.filter((item) => item !== area))}><i className="ms ms-close" aria-hidden="true" /></button></span>)}<button type="button" onClick={() => setSelectedAreas((current) => current.includes(props.serviceArea) ? current : [...current, props.serviceArea])}><i className="ms ms-add" aria-hidden="true" /> {isKorean ? "지역 추가" : "Add area"}</button></div></div><FieldHint>{isKorean ? "필수 · 주 활동 지역을 선택하세요. 세부 지역은 선택 사항입니다." : "Required · choose a main service area. Suburbs are optional."}</FieldHint></section>

    <section className="service-editor-card service-editor-safety"><StepHeading number={7} title={text.safety} help={text.safetyHelp} /><div><article><i className="ms ms-work" aria-hidden="true" /><strong>{text.genuine}</strong></article><article><i className="ms ms-security" aria-hidden="true" /><strong>{text.prohibited}</strong></article><article><i className="ms ms-call" aria-hidden="true" /><strong>{text.contact}</strong></article><article><i className="ms ms-assignment-turned-in" aria-hidden="true" /><strong>{text.reviewed}</strong></article></div></section>
    {props.notice ? <p className="post-create-status" role="status">{props.notice}</p> : null}
    <footer className="service-editor-submit"><label><input type="checkbox" required checked={props.acceptedTerms} onChange={(event) => props.onTermsChange(event.target.checked)} />{text.terms}</label><div><Button className="service-editor-preview-button" variant="secondary" type="button" onClick={props.onPreview}><i className="ms ms-visibility" aria-hidden="true" />{text.preview}</Button><button type="button">{text.save}</button><button type="submit" disabled={props.isSubmitting}>{props.isSubmitting ? (isKorean ? "등록 중…" : "Submitting…") : text.submit}</button></div></footer>
  </form>;
}
